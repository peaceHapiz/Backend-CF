const prisma = require('../../model/model')
const express = require('express')
const router = express.Router()

router.get('/data-guru', async(req,res) => {
    try {
        
    } catch (error) {
        
    }
})